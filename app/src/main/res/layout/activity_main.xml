<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/mainAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:backgroundTint="@color/yellow_primary"
        app:fabCradleRoundedCornerRadius="30dp"
        app:fabCradleMargin="10dp"
        app:fabAnimationMode="slide"
        app:contentInsetEnd="0dp"
        app:contentInsetStart="0dp"
        >
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNavView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:menu="@menu/bottom_nav_menu"
            android:background="@android:color/transparent"
            app:itemIconTint="@drawable/bnv_item_foreground_color"
            app:itemTextColor="@drawable/bnv_item_foreground_color"
            app:itemRippleColor="@color/dark_yellow"
            app:elevation="0dp" />
    </com.google.android.material.bottomappbar.BottomAppBar>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_add"
        app:backgroundTint="@color/blue_secondary_var"
        app:tint="@color/white"
        app:layout_anchor="@+id/mainAppBar"
        app:layout_anchorGravity="bottom|center"
        app:maxImageSize="30dp" />

    <FrameLayout
        android:id="@+id/fragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
